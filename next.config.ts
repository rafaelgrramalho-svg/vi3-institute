import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Para gerar export estático, descomentar as linhas abaixo:
  // output: "export",
  // images: { unoptimized: true },
};

export default nextConfig;
